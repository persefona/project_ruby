<p id="notice"><%= notice %></p>

<p>
  <strong>Ilość głósw na komitet <%= @vote.committee.name %>:</strong>
  <%= @vote.amount %>
</p>

<%= link_to 'Edit', edit_vote_path(@vote) %> |
<%= link_to 'Back', votes_path %>
