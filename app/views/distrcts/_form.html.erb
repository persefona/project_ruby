<%= form_for(@distrct) do |f| %>
  <% if @distrct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@distrct.errors.count, "error") %> prohibited this distrct from being saved:</h2>

      <ul>
      <% @distrct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :voter %><br>
    <%= f.number_field :voter %>
  </div>
  <div class="field">
    <%= f.label :invalid_vote %><br>
    <%= f.number_field :invalid_vote %>
  </div>
  <div class="field">
    <%= f.label :ballot %><br>
    <%= f.number_field :ballot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
